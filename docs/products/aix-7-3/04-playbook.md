# AIX 7.3 — タスクプレイブック（習熟度 × シーン マトリクス）

縦軸 = 習熟度（入門 / 中級 / 上級）、横軸 = シーン。各セルに該当シナリオを「あるものだけ」載せる方針。

| 習熟度＼シーン | 構築・インストール | 日常運用 | バックアップ・リカバリ | 障害対応 | 性能チューニング | マイグレーション・アップグレード | セキュリティ・監査 | スケーリング・拡張 |
|---|---|---|---|---|---|---|---|---|
| **入門** | **● DVD/USB から AIX 7.3 を新規 install**<br>手順: SMS でブートデバイス選択 → BOS Install メニュー → ライセンス受諾 → SWMA 受諾 → 完了後 PKS passphrase 追加<br>cmd: `installp, bosinst.data`<br>[S35, S18] | **● OS レベル/版数確認**<br>手順: oslevel -s で TL/SP まで取得 / oslevel -r で TL のみ / lslpp -L で fileset 一覧<br>cmd: `oslevel, lslpp`<br>[S8]<br><br>**● ユーザ/グループ追加**<br>手順: mkuser → passwd → mkgroup でユーザ/グループ管理。RBAC role を必要に応じて<br>cmd: `mkuser, passwd, mkgroup, lsuser`<br>[S42]<br><br>**● cron で定期ジョブ**<br>手順: crontab -e で編集（root は /var/spool/cron/crontabs/root）<br>cmd: `crontab, at`<br>[S43] | **● mksysb で rootvg バックアップ**<br>手順: mksysb /dev/rmt0 または -i オプションで image.data 含む。tape/file/NIM 全対応<br>cmd: `mksysb, savevg`<br>[S41] | **● errpt でエラー要約確認**<br>手順: errpt（要約） / errpt -a（全文） / errpt -j <error_id>（特定）<br>cmd: `errpt, errlogger`<br>[S45]<br><br>**● snap でサポート提供データ**<br>手順: snap -ac で全カテゴリ採取（snap.pax.gz が既定）<br>cmd: `snap`<br>[S35] | **● topas でリアルタイム性能監視**<br>手順: topas（CPU/メモリ/ディスク/ネット要約） / topas -P（プロセス別）<br>cmd: `topas, vmstat, iostat, netstat`<br>[S25] | — | **● OpenSSH 設定確認**<br>手順: /etc/ssh/sshd_config を編集 → stopsrc / startsrc -s sshd<br>cmd: `sshd, ssh`<br>[S65] | — |
| **中級** | **● NIM サーバ構築 + LPP_SOURCE**<br>手順: bos.sysmgt.nim.master fileset → /usr/lpp/bos.sysmgt/nim/README → LPP_SOURCE 作成 → SPOT 作成（LPP_SOURCE が base+update 混在なら image_data 必須）<br>cmd: `nim, lsnim, mksysb`<br>[S35]<br><br>**● alt_disk_install で並行 install**<br>手順: 代替ディスクに rootvg を複製してから upgrade（巻き戻し容易）<br>cmd: `alt_disk_copy, alt_disk_install`<br>[S18] | **● MPIO 既定値の確認・調整**<br>手順: lsattr -El hdiskN で reserve_policy / algorithm / queue_depth 確認 → chdev -U で動的反映<br>cmd: `lsattr, chdev, lspath`<br>[S35]<br><br>**● LDAP クライアント設定**<br>手順: mksecldap → /etc/security/ldap/ldap.cfg 編集 → secldapclntd 再起動。TL3 SP1 で defaulthomedirectory 等のフィールド拡張<br>cmd: `mksecldap, lsldap, stop-secldapclntd`<br>[S34] | **● NIM 経由 mksysb 取得**<br>手順: NIM master から定義 → クライアント側で smit nim_mksysb → リソースに mksysb 保存<br>cmd: `nim, smit`<br>[S18] | **● errnotify でアラート設定**<br>手順: ODM の errnotify オブジェクト作成（label/class/type で条件指定） → odmadd → 即時有効<br>cmd: `odmadd, errupdate`<br>[S45] | **● ioo / no / schedo / vmo 調整**<br>手順: -L で現状値 → -p -o name=value で永続反映 → -d で既定復元<br>cmd: `ioo, no, schedo, vmo`<br>[S25] | **● 7.1/7.2 → 7.3.3 マイグレーション**<br>手順: premigration script 実行 → hd5 拡張 → base media or NIM でマイグレーション → bos.dsc を最初に installp（base 利用時） → 旧 fileset 置換確認<br>cmd: `premigration, installp`<br>[S35] | **● 監査サブシステム有効化**<br>手順: /etc/security/audit/config 編集 → audit start → audit query で確認<br>cmd: `audit, auditpr`<br>[S32] | **● DLPAR で CPU/メモリ動的増減**<br>手順: HMC から resource add/remove → AIX 側 lsattr で確認 → アプリ再起動なしで反映<br>cmd: `lparstat, lsattr`<br>[S72] |
| **上級** | **● 暗号化 rootvg 構築**<br>手順: BOS install メニューで暗号化対象 LV 選択 → PKS で初期化 → passphrase recovery 追加 → hdcryptmgr で運用<br>cmd: `hdcryptmgr, pksctl`<br>[S35] | **● Cluster Aware AIX リポジトリ管理**<br>手順: NVMe 共有ディスクをクラスタリポジトリに割当て（TL3〜） → clmgr add cluster → RSCT 3.3.0.0 確認<br>cmd: `clmgr, lscluster, mkcluster`<br>[S6] | **● 暗号化LV を含む mksysb の鍵運用**<br>手順: PKS+passphrase は mksysb 復元時に再生成。multibos / alt_disk_copy 利用時も同様<br>cmd: `hdcryptmgr, mksysb`<br>[S35] | **● trace facility でカーネルトレース**<br>手順: AIX 7.3 で trace は root 限定化。trace -f → trcrpt で展開<br>cmd: `trace, trcrpt`<br>[S35]<br><br>**● ldedit による旧 OpenSSL 互換維持**<br>手順: OpenSSL 1.0.2 / 1.1.1 共有オブジェクトは 2025 秋に削除予定。ldedit でリンク再構成<br>cmd: `ldedit, openssl version`<br>[S35] | **● ASO / DSO 自動最適化**<br>手順: asoo で大域有効化 → /etc/security/login.cfg で個別プロセス除外可<br>cmd: `asoo`<br>[S33]<br><br>**● AME（メモリ圧縮）有効化**<br>手順: HMC で AME 設定 → amepat で expansion factor 提案 → AME 反映<br>cmd: `amepat, lparstat`<br>[S25] | **● Live Kernel Update（LKU）**<br>手順: 互換性確認（TL/SP）→ lvupdate.data 編集（ipsec_auto_migrate=yes 等）→ geninstall（LKU mode） or HMC ベース LKU → blackout 短縮（TL3〜）<br>cmd: `geninstall, oslevel -s`<br>[S35]<br><br>**● OpenSSL 1.x → 3.0 移行**<br>手順: 既存 SSL 確認 → /var/ssl/openssl.cnf 退避 → 新 cnf 配置 → legacy provider 必要なら有効化 → アプリ再リンク<br>cmd: `openssl version, openssl ciphers`<br>[S35] | **● Trusted Execution + CHKSHOBJS**<br>手順: TL3 SP1 で CHKSHOBJS ポリシー追加（共有オブジェクト整合性）→ trustchk で適用<br>cmd: `trustchk`<br>[S34]<br><br>**● Domain RBAC 設計**<br>手順: Trusted AIX 廃止の代替。lsroles → mkrole → swrole で fine-grained 権限<br>cmd: `lsroles, mkrole, swrole`<br>[S35] | **● LPM（Live Partition Mobility）**<br>手順: PowerVM 機能。HMC からトリガ。AIX 側は cthags critical resource monitor に注意<br>cmd: `lparstat, hagsglsmconf`<br>[S35]<br><br>**● WPAR + WLM でリソース分離**<br>手順: mkwpar -n → WLM クラス定義 → startwpar → クラス上限を WLM で強制<br>cmd: `mkwpar, lswpar, wlmcntrl`<br>[S75] |

埋まっているセル: **22 / 24**

[← AIX 7.3 トップへ](index.md)
