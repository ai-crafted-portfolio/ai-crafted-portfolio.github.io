# AIX 7.3 — コマンド一覧（網羅・348 件）

AIX マニュアル `Commands Reference Vol1/2/3` および man-page 相当 chunk から `<コマンド名> Command\nPurpose\n` パターンで自動抽出。Hallucinationを避けるため ChromaDB chunk 由来のテキストのみを使用しています。

| コマンド名 | 種別 | 構文 | 用途 | 説明 | 例 | 出典 |
|---|---|---|---|---|---|---|
| accel_decompress | Subroutine | #include <sys/types.h> #include <sys/vminfo.h>   int accel_decompress (void *c_buf, size_t c_len,  void *uc buf, size_t *uc_lenp, int flags) | Decompresses data by using hardware accelerated memory decompression or a slower software | c_buf | — | S3 |
| acct_wpar | Subroutine | — | Enables and disables process accounting. | The acct_wpar subroutine enables the accounting routine when the PathName parameter specifies the | — | S3 |
| acctrpt | Command | — | Generates advanced accounting subsystem data reports. | The acctrpt command displays the advanced accounting statistics. advanced accounting subsystem | — | S7 |
| addrpnode | Command | — | Adds one or more nodes to a peer domain definition. | Before running the addrpnode command: | — | S7 |
| addssys | Subroutine | #include <sys/srcobj.h> #include <spc.h> int addssys ( SRCSubsystem ) struct SRCsubsys *SRCSubsystem; Description | Adds the SRCsubsys record to the subsystem object class. | SRCSubsystem | — | S3 |
| alt_disk_install | Command | Create Alternate Disk  alt_disk_install { -d device \| -C} [ -i image.data] [ -s script ] [ -R resolv_conf] [ -D]  [ -B] [ -V] [ -r] [ -O ] [ | Installs an alternate disk with a mksysb install image or clones the currently running system to an | Note: In AIX 5.3 the alt_disk_install command has been broken up into three commands: | — | S7 |
| amepat | Command | amepat [{{[-c max_ame_cpuusage% ] \| [-C max_ame_cpuusage  ]}\|[  -e startexpfactor  [ :stopexpfactor [ :incexpfactor ]  ]]}][  -F  ][{[-t tgt | Active Memory Expansion Planning and Advisory Tool amepat reports Active Memory Expansion (AME) | Active Memory Expansion Planning and Advisory Tool amepat serves two key functions: | — | S7 |
| artexget | Command | artexget [-v] [-d] [-p \| -r \| -n] [-l {dynamic \| disruptive \| reboot}] [-f {txt \|csv \| xml}] [-m comment] [-V version] [-g categories] [-g l | The artexget command lists the configuration and tuning parameter information from a specified profile | -d | — | S7 |
| audit | Command | — | Controls system auditing. | The audit command controls system audit through several options. Include one option each time you | — | S7 |
| authqry | Command | — | Queries the usage of authorizations over a time period. | The authqry command queries information about the authorizations used by a user over a specified time | — | S7 |
| bdftopcf | Command | — | Converts fonts from Bitmap Distribution Format (bdf) to Portable Compiled Format (pcf). | The bdftopcf command is the font compiler which converts fonts from Bitmap Distribution Format to | 1. To convert fonts into terminal fonts whenever possible, enter: | S7 |
| bellmail | Command | — | Sends messages to system users and displays messages from system users. | The bellmail command with no flags writes to standard output, one message at a time, all stored mail | — | S7 |
| bindprocessor | Command | — | Binds or unbinds the kernel threads of a process to a processor. | The bindprocessor command binds or unbinds the kernel threads of a process, or lists available | — | S7 |
| bootlist | Command | — | Displays and alters the list of boot devices available to the system. | The bootlist command allows the user to display and alter the list of possible boot devices from which | — | S7 |
| bsh | Command | — | The bsh command invokes the Bourne shell. | The bsh command invokes the Bourne shell, an interactive command interpreter and command- | — | S7 |
| bugfiler | Command | — | Automatically stores bug reports in specified mail directories. | The bugfiler command automatically intercepts bug reports, summarizes them, and stores them in the | — | S7 |
| capture | Command | cat [ - q ] [  -r ] [ - s ] [ - S ] [ - u ][ - Z ] [ - n [ - b ] ] [ - v [ - e ] [ - t ] ] [  - \| File ... ] | Allows terminal screens to be dumped to a file. | — | — | S7 |
| cdc | Command | — | Changes the comments in a SCCS delta. | The cdc command changes the Modification Requests (MRs) and comments for the specified SCCS delta | — | S7 |
| cdumount | Command | cdutil { -l \| -r \| -s [ -k ] } [ -q ] [ -h \| -? ] DeviceName | Unmounts a previously mounted file system on a device managed by cdromd. | — | — | S7 |
| certlink | Command | — | certlink links a certificate in a remote repository to a user account. | The certlink command links a certificate in a remote repository to a user account. certlink is very similar | — | S7 |
| chcomg | Command | — | Changes a previously-defined communication group for a peer domain. | The chcomg command changes an existing communication group definition with the name specified by | — | S7 |
| chcosi | Command | — | Manages a Common Operating System Image (COSI). | The chcosi command manages a Common Operating System Image (COSI) created from the mkcosi | — | S7 |
| chdef | Command | — | Changes the default value of the predefined attribute. | The chdef command modifies the default value of a predefined attribute of the specified device type. | — | S7 |
| chedition | Command | — | Allows query or change of the current signature file on the system. | The chedition command can be used to query the current edition of the system. The edition of | — | S7 |
| chgroup | Command | — | Changes attributes for groups. | Attention: Do not use the chgroup command if you have a Network Information Service (NIS) | — | S7 |
| chkey | Command | To Modify the Environment or Profile File Changing the Default Language Setting: chlang [  -u UID \| Uname ] [  -m MsgTransLst \|  -M ] Langua | Changes your encrypting key. | The chlang command is a high-level shell command that changes the language settings for either the | — | S7 |
| chnlspath | Command | chown [  -f ] [ -h ] [  -R ] Owner [ :Group ] { File ... \| Directory ... } chown -R  [  -f ] [ -H \| -L \| -P ] Owner [ :Group ] { File ... \| | Modify the value of the secure NLSPATH system configuration variable. | — | — | S7 |
| chrsrc | Command | — | Changes the persistent attribute values of a resource or a resource class. | The chrsrc command changes the persistent attribute values of a resource or a resource class. By | — | S7 |
| chsmbcred | Command | chsmbcred -s server_name -u user_name [-p password] Description When you run the chsmbcred command, you must specify a server name and a use | Changes the password for a specific server-user entry that is stored in the /etc/smbcred file to allow | — | To change the password that is stored in the /etc/smbcred file for user1 to mount the SMB client file system on the xxx. | S7 |
| chsubserver | Command | — | Changes the contents of the /etc/inetd.conf file or similar system configuration file. | The chsubserver command adds, deletes, or changes entries in the /etc/inetd.conf system configuration | — | S7 |
| chtz | Command | chuser [ -R load_module ] Attribute=Value ... Name Description Attention: Do not use the chuser command if you have a Network Information Se | Changes the TimeZoneInfo (TZ) environment variable in the /etc/environment file. | — | — | S7 |
| chvfs | Command | — | Changes entries in the /etc/vfs file. | The chvfs command changes /etc/vfs file entries by specifying the following fields within | To change the FileSystemHelper for the vfs entry named newvfs, enter: chvfs "newvfs:::/etc/helper/testhelper" The missin | S7 |
| chvg | Command | — | Sets the characteristics of a volume group (VG). | The chvg command changes the characteristics of a VG. You can also use the System Management | — | S7 |
| ckuseracct | Subroutine | #include <login.h> int ckuseracct ( Name,  Mode,  TTY) char *Name; int Mode; char *TTY; | Checks the validity of a user account. | Name | — | S3 |
| clcmd | Command | — | Takes an AIX command and distributes it to a set of nodes that are members of a cluster. | The AIX operating system can operate in a single node or multinode configuration. A multinode | 1. To send the ps command to the oscar-test-dev1 and oscar-test-dev2 nodes in the clusterabc cluster, enter the followin | S7 |
| cmp | Command | — | Compares the contents of two files and reports the first character that differs. | The cmp command compares files designated by the File1 and File2 parameters and writes the results to | 1. To determine whether two files are identical, enter: cmp prog.o.bak prog.o This compares prog.o.bak and prog.o. If th | S7 |
| color_content | Subroutine | #include <curses.h> color_content(Color,  R, G,  B) short   Color; short  *R, * G, * B; | Returns the current intensity of the red, green, and blue (RGB) components of a color. | OK | To obtain the RGB component information for color 10 (assuming the terminal supports at least 11 colors), use:  short *r | S3 |
| colrm | Command | comb [  -o ] [  -s ] [  -c List \|  -p SID ] File Description The comb command writes to standard output a shell procedure that can combine s | Extracts columns from a file. | — | — | S7 |
| cpfile | Subroutine | #include <unistd.h> int cpfile(sfd, dfd, offset, nbytesp, flags) int        sfd; int        dfd; off64_t    offset; | Optimized copy operation of contents from the source file to the destination file. | The cpfile subroutine copies nbytes data from the opened source file (ID specified in the sfd | — | S3 |
| crfs | Command | — | Adds a file system. | The crfs command creates a file system on a logical volume within a previously created volume group. A | — | S7 |
| crvfs | Command | — | Creates entries in the /etc/vfs file. | The crvfs command adds /etc/vfs file entries by specifying fields within the | To create a new vfs entry called newvfs, enter: crvfs "newvfs:4:none:/etc/helpers/newvfshelper" This creates the newvfs | S7 |
| ctags | Command | — | Makes a file of tags to help locate objects in source files. | The ctags command creates a tags file for use with the ex and vi editors from the specified C, Pascal, | — | S7 |
| ctcasd | Daemon | ctcasd [-b] Description The ctcasd daemon is used by the cluster security services library when the RSCT HBA security mechanism is configure | Provides and authenticates the credentials of the RSCT host-based authentication (HBA) and enhanced | — | — | S7 |
| ctsvhbal | Command | ctsvhbal [ [ -d \| -h \| -m \| -s ] \| [ -e msgnum[,msgnum...] ] [ -l { 1 \| 2 \| 3 \| 4 } \| -b ] Description The ctsvhbal command is a verificatio | Displays the possible identities that the local system may use to identify itself in RSCT host-based | — | — | S7 |
| ctsvhbar | Command | ctsvhbar [ [ -d \| -h \| -m \| -s ] \| [ -e msgnum[,msgnum...] ] [ -l { 1 \| 2 \| 3 \| 4 } \| -b ] {hostname \| address} [hostname... \| address...] D | Returns the host name that the RSCT host-based authentication (HBA) security mechanism uses on the | — | — | S7 |
| cut | Command | — | Helps split the lines of a file. | The cut command cuts bytes, characters, or fields from each line of a file and writes these bytes, | — | S7 |
| dadmin | Command | — | Used to query and modify the status of the DHCP server. | The dadmin command enables the DHCP administrator to query and modify the state of DHCP server | — | S7 |
| dbts | Command | — | Debugs a thin server. | The dbts command starts a thin server in the debug mode. The command checks if the thin server was | 1. To debug boot a thin server named lobo that is using a common image named cosi1, enter: dbts lobo A debug boot image | S7 |
| deleteX11input | Command | delta [ -r SID ] [ -s ] [ -n ] [ -g List ] [ -p ] [ -m ModificationRequestList ] [ -y [ Comment ] ] File ... Description The delta command i | Deletes an X11 input extension record from the ODM (Object Data Manager) database. | — | — | S7 |
| delwin | Subroutine | #include <curses.h> int delwin(WINDOW  *win); Description The delwin subroutine deletes win, freeing all memory associated with it. The appl | Deletes a window. | *win | To delete the user-defined window my_window and its subwindow my_sub_window, enter: WINDOW *my_sub_window, *my_window; d | S3 |
| devrsrv | Command | — | Queries and breaks the single-path and persistent reservations on a device. | The devrsrv command queries and breaks the single-path and persistent reservations on the device. | — | S7 |
| dhcpaction | Command | — | Provides a script that runs every time that a client updates its lease. | The dhcpaction command provides methods to update the DNS server by calling the nsupdate | — | S7 |
| dhcpcd6 | Daemon | To start a DHCPv6 client by using the System Resource Controller (SRC): startsrc -s dhcpcd6 [-a Argument] ... To start a DHCPv6 client witho | Implements a Dynamic Host Configuration Protocol for IPv6 (DHCPv6) client. Obtains IPv6 addresses and | -f ConfigurationFileName | — | S7 |
| diagrpt | Command | diagsetrto [ [ -a on \| off ] [ -d on \| off ] [ -l Size ] [ -m on \| off ] [ -n Days ] [ -p on \| off ] ] | Displays previous diagnostic results. | — | — | S7 |
| diffmk | Command | — | Marks differences between files. | The diffmk command compares the English-language file specified by the File1 parameter with the file | — | S7 |
| dircmp | Command | — | Compares two directories and the contents of their common files. | The dircmp command compares the two directories specified by the Directory1 and Directory2 | 1. To summarize the differences between the files in two directories, type the following: dircmp proj.ver1 proj.ver2 Thi | S7 |
| dirname | Subroutine | #include <libgen.h> char *dirname (path) char *path Description Given a pointer to a character string that contains a file system path name, | Report the parent directory name of a file path name. | path | A simple file name and the strings "." and ".." all have "." as their return value. Input string Output string /usr/lib | S3 |
| dlclose | Subroutine | #include <dlfcn.h> char *dlerror(void); | Closes and unloads a module loaded by the dlopen subroutine. | — | — | S3 |
| dnssec-importkey | Command | dnssec-importkey [-K directory] [-L ttl] [-P date/offset] [-P sync date/offset] [-D date/offset] [-D sync date/offset] [-h] [-v level] [-V] | Imports the domain name server key (DNSKEY) records from external systems so that the records can be | — | — | S7 |
| dnssec-signzone | Command | — | Domain name system security extensions (DNSSEC) zone signing tool. | The dnssec-signzone command signs a zone. It generates next secure record (NSEC) and resource | — | S7 |
| dnssec-verify | Command | — | Verifies the Domain name system security extensions (DNSSEC) zone. | The dnssec-verify command verifies that a zone is fully signed for each algorithm in the Domain Name | — | S7 |
| dosdir | Command | — | Lists the directory for DOS files. | The dosdir command displays information about the specified DOS files or directories. If you specify a | — | S7 |
| dslpsearch | Command | — | Search directory for print system objects on a System V print subsystem. | The dslpsearch command allows users and administrators to search the directory for print system | — | S7 |
| dtsession | Command | — | Manages a CDE session. | The dtsession command provides session management functionality, compliant with ICCCM 1.1, during | — | S7 |
| du | Command | — | Summarizes disk usage. | The du command displays the number of blocks used for files. If the File parameter specified is actually a | — | S7 |
| dumpctrl | Command | — | Manages system dumps and live dumps. | There are two types of dump components: | — | S7 |
| efskstoldif | Command | — | Prints certain EFS users or groups keystore that are defined locally to stdout in ldif format. | The efskstoldif command reads data from locally defined EFS users or groups keystore files and prints | — | S7 |
| entstat | Command | — | Shows ethernet device driver and device statistics. | The entstat command displays the statistics gathered by the specified Ethernet device driver. The user | — | S7 |
| errctrl | Command | errctrl [ -nru ] ComponentSelector ... subcommand ... errctrl -p [ -ru ] ComponentSelector ... subcommand ... errctrl -P [ -ru ] ComponentSe | Modifies or displays the error-checking attributes of system components. Persistent attribute values can | The errctrl command modifies or displays the error-checking attribute values of some or all components. | — | S7 |
| errdead | Command | — | Extracts error records from a system dump or live dump. | The errdead command extracts error records from a system dump or live dump containing the internal | To capture error log information from a dump image that resides in the /var/adm/ras/vmcore.0 file, enter: /usr/lib/errde | S7 |
| errdemon | Daemon | — | Starts error logging daemon (errdemon) and writes entries to the error log. | The error logging daemon reads error records from the /dev/error file and creates error log entries in | — | S7 |
| errmsg | Command | — | Adds a message to the error log message catalog. | The errmsg command updates and displays the error-log message catalog containing the Error | — | S7 |
| fc | Command | — | Processes the command history list. | The fc command displays the contents of your command history file or invokes an editor to modify and | — | S7 |
| fcteststk | Command | — | Test for the presence of a First Failure Data Capture Error Stack environment. | fcteststk can be called by any application program that wishes to use the FFDC Error Stack to test if | — | S7 |
| fmemopen | Subroutine | #include <stdio.h> FILE *fmemopen (void *restrict buf, size_t size, const char *restrict mode); Description The fmemopen subroutine associat | Opens a memory buffer stream. | — | — | S3 |
| fmtmsg | Subroutine | #include <fmtmsg.h> int fmtmsg (long Classification, const char *Label, int Severity, cont char *Text; | Display a message in the specified format on standard error, the console, or both. | The fmtmsg subroutine can be used to display messages in a specified format instead of the traditional | — | S3 |
| folders | Command | — | Lists all folders and messages in the mail directory. | The folders command lists all folders and messages in your mail directory. This command is equivalent | — | S7 |
| frcactrl | Command | — | Controls and configures FRCA. | The frcactrl command controls and configures the FRCA kernel extension. The kernel extension must | — | S7 |
| freehostent | Subroutine | #include <netdb.h> void freehostent (ptr) struct hostent * ptr; Description The freehostent subroutine frees any dynamic storage pointed to | To free memory allocated by getipnodebyname and getipnodebyaddr. | The freelocale subroutine releases the resources allocated for a locale object that is returned by a call to | — | S3 |
| ftok | Subroutine | #include <sys/types.h> #include <sys/ipc.h> key_t ftok ( Path,  ID) char *Path; int ID; | Generates a standard interprocess communication key. | Path | — | S3 |
| ftp | Command | — | Transfers files between a local and a remote host. | The ftp command uses the File Transfer Protocol (FTP) to transfer files between the local host and a | — | S7 |
| ftpd | Daemon | — | Provides the server function for the Internet FTP protocol. | The /usr/sbin/ftpd daemon is the DARPA Internet File Transfer Protocol (FTP) server process. The ftpd | — | S7 |
| fwtmp | Command | /usr/sbin/acct/fwtmp [ -i ] [ -c ] [ -X ] [ -L ] Description The fwtmp command manipulates the accounting records by reading binary records | Manipulates connect-time accounting records by reading binary records in wtmp format from standard | -i | 1. To convert a binary record in wtmp format to an ASCII record called dummy.file, enter: /usr/sbin/acct/fwtmp < /var/ad | S7 |
| fxfer | Command | — | Transfers files between a local system and a host computer connected by HCON. | The fxfer command transfers files between local system and mainframe hosts connected by the Host | — | S7 |
| genld | Command | genld [ -h \| -l [ -d ] ] [ -a Area ] [-u] Description For each process currently running, the genld command prints a report consisting of th | The genld command collects the list of all processes currently running on the system, and optionally | -a Area | • To obtain the list of loaded objects for each running process, enter the following command: genld -l • To obtain the l | S7 |
| gentun | Command | — | Creates a tunnel definition in the tunnel database. | The gentun command creates a definition of a tunnel between a local host and a tunnel partner host. The | — | S7 |
| genxlt | Command | — | Generates a code set conversion table for use by the lconv library. | The genxlt command reads a source code set conversion table file from standard input and writes | — | S7 |
| get_malloc_log | Subroutine | #include <malloc.h> struct malloc_log* get_malloc_log_live (addr) void *addr; Description The get_malloc_log_live subroutine provides access | Retrieves information about the malloc subsystem. | — | — | S3 |
| getauthattrs | Subroutine | #include <usersec.h> int getauthattrs(Auth, Attributes, Count)     char *Auth;     dbattr_t *Attributes;     int Count; | Retrieves multiple authorization attributes from the authorization database. | attr_name | — | S3 |
| getconfattrs | Subroutine | #include <usersec.h> #include <userconf.h> int getconfattrs (Sys, Attributes, Count) char * Sys; dbattr_t * Attributes; | Accesses system information in the system information database. | The getconfattrs subroutine accesses system configuration information. | — | S3 |
| getprivname | Subroutine | #include <userpriv.h> #include <sys/priv.h> char *getprivname(int priv) Description The getprivname subroutine converts a given privilege bi | Converts a privilege bit into a readable string. | priv | — | S3 |
| getprocs | Subroutine | #include <procinfo.h> #include <sys/types.h>  int getprocs ( ProcessBuffer,  ProcessSize,  FileBuffer,  FileSize,  IndexPointer,  Count) str | Gets process table entries. | The getprocs subroutine returns information about processes, including process table information | — | S3 |
| getsubsvr | Subroutine | #include <sys/srcobj.h> #include <spc.h> int getsubsvr(  SubserverName,  SRCSubserver) char *SubserverName; struct SRCSubsvr *SRCSubserver; | Reads a subsystem record. | char | — | S3 |
| getsyx | Subroutine | #include <curses.h> getsyx(Y, X)  int * Y, * X; Description The getsyx subroutine retrieves the current coordinates of the virtual screen cu | Retrieves the current coordinates of the virtual screen cursor. | X | — | S3 |
| getuinfo | Subroutine | char *getuinfo ( Name) char *Name; Description The getuinfo subroutine finds a value associated with a user. This subroutine searches a user | Finds a value associated with a user. | Name | — | S3 |
| glob | Subroutine | #include <glob.h> int glob (Pattern, Flags, (Errfunc)(), Pglob) const char  *Pattern; int  Flags; int  *Errfunc (Epath, Eerrno) | Generates path names. | The glob subroutine constructs a list of accessible files that match the Pattern parameter. | — | S3 |
| globfree | Subroutine | #include <glob.h> void globfree ( pglob) glob_t *pglob; Description The globfree subroutine frees any memory associated with the pglob param | Frees all memory associated with the pglob parameter. | pglob | — | S3 |
| grap | Command | — | Typesets graphs to be processed by the pic command. | The grap command processes grap language input files and generates input to the pic command. The | — | S7 |
| haemunlkrm | Command | — | Unlocks and starts a resource monitor. | If the event management daemon cannot successfully start a resource monitor after three attempts | — | S7 |
| hagsns | Command | — | Gets group services name server information. | Use the hagsns command to query the status of the group services nameserver. | — | S7 |
| HBA_GetNumberOfAdapters | Subroutine | #include <sys/hbaapi.h> HBA_UINT32 HBA_GetNumberOfAdapters () Description The HBA_GetNumberOfAdapters subroutine returns the number of HBAs | Returns the number of adapters discovered on the system. | The HBA_GetFcpPersistentBindingV2 function returns persistent bindings between an FCP target and a | — | S3 |
| HBA_ScsiInquiryV2 | Subroutine | — | Sends a SCSI INQUIRY command to a remote end port. | The HBA_ScsiInquiryV2 function sends a SCSI INQUIRY command to a remote end port. | — | S3 |
| HBA_SendRNIDV2 | Subroutine | — | Issues an RNID ELS to another FC_Port requesting a specified Node Identification Data Format. | The HBA_SendRNIDV2 function issues an RNID ELS to another FC_Port requesting a specified Node | — | S3 |
| hpmcount | Command | — | Measures application performance. | The hpmcount command provides the execution wall clock time, hardware performance counters | — | S7 |
| hps_dump | Command | — | Dumps contents of Network Terminal Accelerator (NTX) adapter memory to a host file. | The hps_dump command uses the loader interface to upload all of the memory from the adapter board | — | S7 |
| ibm5585H-T | Command | ibm5587G [ -FDirectory] [ -quietly] [File ...] | Processes troff command output for the IBM 5585H-T printer. | — | — | S7 |
| imake | Command | — | C preprocessor interface to the make command. | The imake command generates Makefiles from a template, a set of cpp macro functions, and a per- | — | S7 |
| indent | Command | — | Reformats a C language program. | The indent command reformats a C program as specified by flags entered with the command. | — | S7 |
| infocmp | Command | — | Manages terminfo descriptions. | The infocmp command manages terminfo descriptions. You can use this command to: | — | S7 |
| installbsd | Command | — | Installs a command (BSD version of the install command). | The installbsd command installs the file specified by the BinaryFile parameter by moving it to a file or | To install a new command called fixit, enter: installbsd  -c o mike fixit /usr/bin This command sequence installs a new | S7 |
| installp | Command | — | Installs available software products in a compatible installation package. | — | — | S7 |
| inurid | Command | inurid [ -q \| -r ] Description The inurid command is used to remove files stored in the inst_root directories of installed software. The nam | Removes information that is used for the installation of diskless or dataless clients and workload | -q | — | S7 |
| inusave | Command | inusave ListFile ProductName Description The inusave command saves the files and archived files that are listed in the file specified by the | Saves files that are installed or updated during an installation procedure. This command is used by the | — | — | S7 |
| inutoc | Command | inutoc [ Directory ] Description The inutoc command creates the .toc file in Directory. If a .toc file already exists, it is recreated with | Creates a .toc file for directories that have backup format file install images. This command is used by the | INUBADIR | 1. To create the .toc file for the /usr/sys/inst.images directory, enter: inutoc 2. To create a .toc file for the /tmp/i | S7 |
| inuumsg | Command | inuumsg Number [ Argument1 ] [ , Argument2 ] [ , Argument3 ] [ , Argument4 ] Description The inuumsg command displays error or diagnostic me | Displays specific error or diagnostic messages provided by a software product's installation procedures. | 0 | To see error message number 3, enter: inuumsg 3 Files Item Description | S7 |
| keypasswd | Command | — | keypasswd manages the passwords which are used to access a user's private keystore. | The keypasswd command allows a user to change the password of a private keystore. The user will | 1. To change the password of the default private keystore that is owned by Bob, enter: $ keypasswd where the invoker is | S7 |
| keysvrmgr | Command | keysvrmgr action -t server_type [-h] { -a attribute=value ... } server_name Description An encryption key server is used to securely store e | Manages the Object Data Manager (ODM) database entries that are associated with the encryption key | — | — | S7 |
| krlogind | Daemon | — | Provides the server function for the rlogin command. | The /usr/sbin/krlogind daemon is the server for the rlogin remote login command. The server provides a | — | S7 |
| krs_alloc | Subroutine | — | Allocates a resource set and returns its handle. | The krs_alloc subroutine allocates a resource set and initializes it according to the information specified | — | S21 |
| krs_getrad | Subroutine | — | Returns a system resource allocation domain (RAD) contained in an input resource set. | The krs_getrad subroutine returns a system RAD at a specified system detail level and index. | — | S21 |
| LAPI_Gfence | Subroutine | — | Enforces order on LAPI calls across all tasks and provides barrier synchronization among them. | Type of call: collective data synchronization (blocking) | — | S3 |
| LAPI_Init | Subroutine | — | Initializes a LAPI context. | Type of call: Local initialization | — | S3 |
| LAPI_Rmw | Subroutine | — | Provides data synchronization primitives. | Type of call: point-to-point communication (non-blocking) | — | S3 |
| LAPI_Setcntr | Subroutine | — | Used to set a counter to a specified value. | Type of call: Local counter manipulation | — | S3 |
| LAPI_Util | Subroutine | — | Serves as a wrapper function for such data gather/scatter operations as registration and reservation, | Type of call: Data gather/scatter program (DGSP), UDP port information, and lock sharing utilities | — | S3 |
| LAPI_Waitcntr | Subroutine | — | Waits until a specified counter reaches the value specified. | Type of call: local progress monitor (blocking) | — | S3 |
| lastcomm | Command | — | Displays information about the last commands executed. | The lastcomm command displays information, in reverse chronological order, about all previously | 1. To display information about all previously executed commands recorded in the /var/adm/pacct file, enter: lastcomm 2. | S8 |
| lbxproxy | Command | — | Low BandWidth X proxy. | The lbxproxy command accepts client connections, multiplexes them over a single connection to the X | — | S8 |
| ldedit | Command | — | Modifies an XCOFF executable file header. | You can use the ldedit command to modify various fields in an XCOFF header or the auxiliary header | — | S8 |
| ldtbread | Subroutine | #include <stdio.h> #include <ldfcn.h> int ldtbread ( ldPointer,  SymbolIndex,  Symbol) LDFILE *ldPointer; long SymbolIndex; | Reads an indexed symbol table entry of a common object file. | The ldtbread subroutine reads the symbol table entry specified by the SymbolIndex parameter of the | — | S3 |
| learn | Command | — | Provides computer-aided instruction for using files, editors, macros, and other features. | The learn command provides computer-aided instruction for using files, editors, macros, and other | — | S8 |
| lecstat | Command | lecstat [ -a -c -q -r -s -t -v ] Device_Name Description This command displays ATM LAN Emulation Client (LEC) operational information gather | Displays operational information about an Asynchronous Transfer Mode network protocol (ATM) Local | Device_Name | — | S8 |
| listea | Subroutine | — | Lists the extended attributes associated with a file. | Extended attributes are name:value pairs associated with the file system objects (such as files, | — | S3 |
| locktrace | Command | — | Controls kernel lock tracing. | The locktrace command controls which kernel locks are being traced by the trace subsystem. The default | 1. To start tracing the SEM_LOCK_CLASS, enter the following command: locktrace -s SEM_LOCK_CLASS 2. To stop all lock tra | S8 |
| loginfailed | Subroutine | #include <usersec.h> int loginfailed ( User,  Host,  Tty, Reason) char *User; char *Host; char *Tty; | Records an unsuccessful login attempt. | The loginfailed subroutine performs the processing necessary when an unsuccessful login attempt | — | S3 |
| lpar_set_resources | Subroutine | #include  <sys/dr.h> int lpar_set_resources ( lpar_resource_id,lpar_resource ) int lpar_resource_id; void *lpar_resource; Description | Modifies the calling partition's characteristics. | lpar_resource_id | — | S3 |
| lphistory | Command | • To list a particular number of previously-issued commands: – On the local node: lphistory [ -u user_ID ] [ -m mapped_ID ] [ -C command_nam | Displays or clears the history list of least-privilege (LP) commands that have been run during the current | The lphistory command lists the history of LP commands that have been run by the least-privilege | — | S8 |
| lsCCadmin | Command | To Display Specific Data on all Systems lscfg [ -v ] [ -p ] [ -s ] [ -l Name ] Description If you run the lscfg command without any flags, i | Display the name of the current Common Criteria enabled System Administrative Host. | — | — | S8 |
| lsdom | Command | — | Displays domain attributes. | The lsdom command displays the attributes of the domain, which is defined from the domain database. | — | S8 |
| lslpcmd | Command | — | Lists information about the least-privilege (LP) resources on one or more nodes in a domain. | The lslpcmd command displays information about LP resources on one or more nodes in a domain. LP | — | S8 |
| lslprsacl | Command | — | Displays the access controls for the least-privilege (LP) Resource Shared ACL. | The lslprsacl command displays the access control list (ACL) that is associated with the least-privilege | — | S8 |
| lslv | Command | — | Displays information about a logical volume. | The lslv command displays the characteristics and status of the LogicalVolume or lists the logical volume | — | S8 |
| lssensor | Command | lssensor [-m] [ -a \| -n host1[,host2…] \| -N { node_file "–" } ] [ -l \| -t \| -d \| -D delimiter ] [-x] [-h] [ -v \| -V ] [ -A \| sensor_name1 [ | Displays information about sensors and microsensors that are defined to the resource monitoring and | Name | — | S8 |
| lsvg | Command | — | Displays information about volume groups. | The lsvg command displays information about volume groups. If you use the volumegroup parameter, | — | S8 |
| lvm_querylv | Subroutine | #include <lvm.h> int lvm_querylv ( LV_ID,  QueryLV,  PVName) struct lv_id *LV_ID; struct querylv **QueryLV; char *PVName; | Queries a logical volume and returns all pertinent information. | lvname | — | S3 |
| lvmo | Command | — | Manages lvm pbuf tunable parameters. | The lvmo command sets or displays pbuf tuning parameters. The equal sign can be used to set a | — | S8 |
| lvupdateInit | Command | To add an entry to the /etc/inittab file in the surrogate partition, use the following syntax: lvupdateInit -a [-i Identifier] { [ Identifie | Manages the list of entries to be added to the /etc/inittab file that is used to start the surrogate | The Live Update operation creates a customized root volume group (rootvg) to start the surrogate | — | S8 |
| lvupdateSafeKE | Command | lvupdateSafeKE  [ -a kext_path [-p] \| -r kext_path \| -l ] Description During a Live Update operation, a new logical partition (LPAR) is dyna | The lvupdateSafeKE command is a utility that manipulates the list of safe kernel extensions for the AIX | kext_path | — | S8 |
| macref | Command | — | Produces a cross-reference listing of macro files. | The macref command reads the named English-language files (which are assumed to be nroff or troff | — | S8 |
| mark | Command | — | Creates, modifies, and displays message sequences. | The mark command creates, deletes, adds, and lists messages in a sequence. The mark command by | — | S8 |
| mhmail | Command | — | Sends or receives mail. | The mhmail command composes, sends, and files messages. To file a message, enter the mhmail | 1. To receive new mail and file it into the default mail folder, $USER/Mail/inbox, enter: mhmail The system displays a m | S8 |
| MIO_close | Subroutine | #include <libmio.h> int MIO_close (FileDescriptor) int FileDescriptor; Description This subroutine is an entry point of the MIO library. Use | Close a file descriptor through the MIO library. | — | — | S3 |
| mkcifsmnt | Command | — | Adds a CIFS mount to the /etc/filesystems file and performs the mount. | The mkcifsmnt command constructs a CIFS entry that is appended to the /etc/filesystems file. It | — | S8 |
| mkcimreg | Command | To register a class: mkcimreg [-I include_directory...] [-f] [-h] definition_file... To register a provider: mkcimreg [-I include_directory. | Registers Common Information Model (CIM) classes and Common Manageability Programming Interface | The mkcimreg command registers Common Information Model (CIM) classes and Common | — | S8 |
| mkitab | Command | — | Makes records in the /etc/inittab file. | The mkitab command adds a record to the /etc/inittab file. The Identifier:RunLevel:Action:Command | — | S8 |
| mkramdisk | Command | — | Creates a RAM disk using a portion of RAM that is accessed through normal reads and writes. | The mkramdisk command is shipped as part of bos.rte.filesystems, which allows the user to create a | — | S8 |
| mksecldap | Command | To set up a server mksecldap -s -a adminDN -p adminpasswd -S schematype [ -d baseDN ] [ -n port ] [ -k  SSLkeypath ] [ -w SSLkeypasswd ] [ - | Sets up an AIX system as a Lightweight Directory Access Protocol (LDAP) server or client for security | The mksecldap command can be used to set up the IBM Directory servers and clients for security | — | S8 |
| mkwpar | Command | — | Creates a system workload partition (WPAR), or a WPAR specification file. | The mkwpar command builds the infrastructure to prepare a system workload partition for use. This | — | S8 |
| mprotect | Subroutine | #include <sys/types.h> #include <sys/mman.h> int mprotect ( addr,  len,  prot) void *addr; size_t len; | Modifies access protections for memory mapping or shared memory. | The mprotect subroutine modifies the access protection of a mapped file or shared memory region | — | S3 |
| mq_open | Subroutine | #include <mqueue.h> mqd_t mq_open (name, oflag [mode, attr]) const char *name; int oflag; mode_t mode; | Opens a message queue. | The mq_open subroutine establishes a connection between a process and a message queue with a | — | S3 |
| mq_setattr | Subroutine | #include <mqueue.h> int mq_setattr (mqdes, mqstat, omqstat) mqd_t mqdes; const struct mq_attr *mqstat; struct mq_attr *omqstat; | Sets message queue attributes. | mqdes | — | S3 |
| mrouted | Daemon | — | Forwards a multicast datagram. | The mrouted daemon is an implementation of the Distance Vector Multicast Routing Protocol (DVMRP), | — | S8 |
| mvdir | Command | — | Moves (renames) a directory. | The mvdir command renames directories within a file system. To use the mvdir command, you must have | To rename or move a directory to another location, enter: mvdir appendixes manual If manual does not exist, this renames | S8 |
| nanosleep | Subroutine | #include <time.h> int nanosleep (rqtp, rmtp) const struct timespec *rqtp; struct timespec *rmtp; Description | Causes the current thread to be suspended from execution. | rqtp | — | S3 |
| ndpd-host | Daemon | — | Neighbor Discovery Protocol (NDP) daemon for a host. | The ndpd-host command manages the Neighbor Discovery Protocol (NDP) for nonkernel activities, such | — | S8 |
| netrule | Command | — | Adds, removes, lists, or queries rules, flags and security labels for interfaces and hosts. | The netrule command lists, queries, adds and removes rule specifications for interfaces and hosts. The | — | S8 |
| newaliases | Command | newform [ -s ] [ -f ] [ -a [ Number ] ] [ -b [ Number ] ] [ -c [ Character ] ] [ -e [ Number ] ] [ -i [ TabSpec ] ] [ -l [ Number ] ] [ -o [ | Builds a new copy of the alias database from the mail aliases file. | — | — | S8 |
| newpass | Subroutine | #include <usersec.h> #include <userpw.h> char *newpass( Password) struct userpw *Password; Description | Generates a new password for a user. | — | — | S3 |
| newterm | Subroutine | #include <curses.h> SCREEN *newterm(  Type,   OutFile, InFile) char *Type; | Initializes curses and its data structures for a specified terminal. | The newterm subroutine initializes curses and its data structures for a specified terminal. Use this | 1. To initialize curses on a terminal represented by the lft device file as both the input and output terminal, open the | S3 |
| nfs.clean | Command | — | Stops NFS and NIS operations. | The /etc/nfs.clean command is used to shut down operations of NFS, NIS, or both. This script is used | 1. To stop all NFS and NIS daemons, type: /etc/nfs.clean 2. To stop only NFS, type: /etc/nfs.clean -t nfs 3. To stop onl | S8 |
| nfs4cl | Command | — | Displays or modifies current NFSv4 statistics and properties. | Use the nfs4cl command to display all the fsid information on the client or modify filesystem options of an | — | S8 |
| nfso | Command | — | Manages the tuning parameters of the Network File System (NFS). | The nfso command configures the NFS tuning parameters. The nfso command sets or displays the | — | S8 |
| nimadapters | Command | — | Defines Network Installation Management (NIM) secondary adapter definitions from a stanza file. | The nimadapters command parses a secondary adapters stanza file to build the files required to add | — | S8 |
| nimadm | Command | Perform alternate disk migration nimadm -l lpp_source -c NIMClient -s SPOT -d TargetDisks [ -a PreMigrationScript ] [ -b  installp_bundle] [ | The nimadm (Network Installation Manager alternate disk migration) command is a utility that allows the | — | — | S8 |
| nmon | Command | — | Displays local system statistics in interactive mode and records system statistics in recording mode. | The nmon command displays and records local system information. The command can run either in | — | S8 |
| nohup | Command | — | Runs a command without hangups. | The nohup command runs the command specified by the Command parameter and any related Arg | 1. To run a command in the background after you log off, enter: $ nohup find / -print & After you enter this command, th | S8 |
| nslookup | Command | — | Queries the internet Domain Name System (DNS) interactively. | The nslookup command queries internet domain name servers in the following two modes: | — | S8 |
| odm_unlock | Subroutine | #include <odmi.h> int odm_unlock ( LockID) int LockID; Description The odm_unlock subroutine releases a previously granted lock on a path na | Releases a lock put on a path name. | LockID | — | S3 |
| odmshow | Command | /usr/bin/on [ -i ] [ -d ] [ -n ] Host Command [ Argument ... ] Description The on command executes commands on other systems in an environme | Displays an object class definition on the screen. | — | — | S8 |
| OS_install | Command | — | Performs network installation operations on OS_install objects. | The OS_install command performs a network installation operation on an OS_install object. The | — | S8 |
| pagesize | Command | — | Displays the system page size. | The pagesize command prints the size, in bytes, of a page of memory, as returned by the getpagesize | 1. To obtain the size system page, enter: pagesize The system returns the number of bytes, such as 4096. 2. To print the | S8 |
| paginit | Command | — | Authenticate a user and create a PAG association. | The paginit command authenticates username (by default, the user issuing the command) and creates an | paginit -R FPKI  The user is authenticated using the registry FPKI, which is defined in the /usr/lib/security/methods.cf | S8 |
| pam_chauthtok | Subroutine | #include <security/pam_appl.h> int pam_chauthtok (PAMHandle, Flags) pam_handle_t *PAMHandle; int Flags; Description | Changes the user's authentication token (typically passwords). | PAMhandle | — | S3 |
| Parameter | Command | — | ip6srcrouteforward | — | — | S32 |
| passwdexpired | Subroutine | — | Checks the user's password to determine whether it is expired. | The passwdexpired subroutine checks a user's password to determine whether it is expired. The | — | S3 |
| pause | Subroutine | #include <unistd.h> int pause (void) Description The pause subroutine suspends the calling process until it receives a signal. The signal mu | Suspends a process until a signal is received. | The pcap_close subroutine closes the files associated with the packet capture descriptor and deallocates | — | S3 |
| pcap_datalink | Subroutine | #include <pcap.h> int pcap_datalink(pcap_t * p); Description The pcap_datalink subroutine returns the link layer type of the packet capture | Obtains the link layer type (data link type) for the packet capture device. | p | — | S3 |
| pcap_open_live | Subroutine | #include <pcap.h> pcap_t *pcap_open_live( const char * device, const int snaplen, const int promisc, const int to_ms, char * ebuf); Descript | Opens a network device for packet capture. | device | — | S3 |
| pdmode | Command | — | Invokes a command in the virtual or real partitioned, directory-access mode. | The pdmode command allows you to invoke a command that you specify using the command parameter | 1. To get the partitioned directory access mode, enter: pdmode 2. To run the ls command in the virtual mode, enter: pdmo | S8 |
| penable | Command | — | Enables or reports the availability of login ports. | The penable command enables normal ports. Normal ports are asynchronous and only allow users to log | To enable all normal ports listed in the /etc/inittab file, enter: penable -a Files Table 117. Files Item | S8 |
| perfstat_cpu_total | Subroutine | #include <libperfstat.h> int perfstat_cpu_total (name, userbuff, sizeof_struct, desired_number) perfstat_id_t *name; perfstat_cpu_total_t *u | Retrieves global processor usage statistics. | The perfstat_cpu_total subroutine returns global processor usage statistics in a | — | S3 |
| perfstat_fcstat | Subroutine | #include <libperfstat.h> int perfstat_fcstat (name, userbuff, sizeof_struct, desired_number) perfstat_id_t *name; perfstat_fcstat_t *userbuf | Retrieves the statistics of a Fibre Channel (FC) adapter. | The perfstat_fcstat subroutine retrieves the statistics of one or more FC adapters. The same function is | — | S3 |
| perfstat_memory_page | Subroutine | #include <libperfstat.h> int perfstat_memory_page ( psize, userbuff, sizeof_userbuff, desired_number ) perfstat_psize_t *psize; perfstat_mem | Retrieves usage statistics for multiple page sizes. | The perfstat_memory_page subroutine returns the statistics corresponding to the different page sizes. | — | S3 |
| perfstat_memory_page_wpar | Subroutine | #include <libperfstat.h> int perfstat_memory_page_wpar ( name, psize, userbuff, sizeof_userbuff, desired_number ) perfstat_id_wpar_t *name; | Retrieves use statistics for multiple page size for workload partitions (WPAR) | The perfstat_memory_page_wpar subroutine returns the page statistics for the WPAR in | — | S3 |
| perfstat_node_list | Subroutine | #include <libperfstat.h> int perfstat_node_list  ( name, userbuff, sizeof_userbuff, desired_number) perfstat_id_node_t *name; perfstat_node_ | Retrieves the list of nodes in a cluster. | The perfstat_node_list subroutine returns the list of nodes in a perfstat_node_t structure. | — | S3 |
| perfstat_ssp_ext | Subroutine | #include <libperfstat.h> int perfstat_ssp_ext (name, userbuff, sizeof_struct, desired_number, ssp_flag) perfstat_ssp_id_t * name; perfstat_s | Retrieves the tier, failure group, physical volume, and node data that are associated with shared storage | The perfstat_ssp_ext subroutine retrieves the SSP statistics on the tier, failure group, and the | — | S3 |
| perfstat_virtualdisktarget | Subroutine | #include <libperfstat.h> int perfstat_virtualdisktarget (name, userbuff, sizeof_struct, desired_number) perfstat_id_t * name; perfstat_disk_ | Retrieves the Virtual Target Device (VTD) usage statistics in Virtual I/O Server (VIOS). | name | — | S3 |
| phold | Command | — | Disables or reports the availability of login ports on hold. | The phold command disables a set of login ports. The phold command allows logged-in users to | To list the ports that are on hold, enter: phold Files Table 121. Files Item | S8 |
| ping | Command | — | Sends an ECHO_REQUEST to a network host. | The ping command sends an Internet Control Message Protocol (ICMP) ECHO_REQUEST to receive an | — | S8 |
| pioburst | Command | — | Generates burst pages (header and trailer pages) for printer output. | The pioburst command retrieves prototype text for a burst page from the file specified by the TextFile | — | S8 |
| pioout | Command | — | Printer backend's device driver interface program. | The pioout command is at the end of pipelines invoked by the piobe command (the print job manager) to | — | S8 |
| pkgchk | Command | — | Checks the accuracy of an installation. | pkgchk Checks the accuracy of installed files or by use of the -l flag, displays information about package | — | S8 |
| pkginfo | Command | — | Displays software package and/or set information. | pkginfo Displays information about software packages or sets that are installed on the system (as | — | S8 |
| pksctl | Command | — | Performs administrative operations on the user-space Platform keystore (PKS) framework. | The pksctl command is used to perform administrative operations that relate to the PKS framework at | — | S8 |
| plock | Subroutine | #include <sys/lock.h> int plock ( Operation) int Operation; Description The plock subroutine allows the calling process to lock or unlock it | Locks the process, text, or data in memory. | Operation | — | S3 |
| pm_clear_ebb_handler | Subroutine | #include <pmapi.h> int pm_clear_ebb_handler (void ** old_handler, void ** old_data_area) Description The pm_clear_ebb_handler subroutine cle | Clears the Event-Based Branching (EBB) facility configured for the calling thread. | old_handler | — | S3 |
| pm_enable_bhrb | Subroutine | #include <pmapi.h> int pm_enable_bhrb (pm_bhrb_ifm_t ifm_mode) Description The pm_enable_bhrb subroutine enables the BHRB instructions such | Enables all Branch History Rolling Buffer (BHRB) related instructions such as clrbhrb and mfbhrb in the | ifm_mode | — | S3 |
| pm_get_program_pthread | Subroutine | #include <pmapi.h>   int pm_set_program_pthread ( pid,  tid,  ptid,  *prog)  pid_t pid;  tid_t tid; ptid_t ptid; | Retrieves the Performance Monitor settings for a target pthread. | The pm_get_program_pthread subroutine retrieves the Performance Monitor settings for a target | — | S3 |
| pm_get_wplist | Subroutine | #include <pmapi.h>int pm_get_wplist (*name, *wp_list, *size) const char *name;  pm_wpar_ctx_info_t *wp_list;  int *size; Description | Retrieves the list of available workload partition contexts for Performance Monitoring. | name | — | S3 |
| pm_set_program_pgroup | Subroutine | #include <pmapi.h>   int pm_set_program_pgroup ( pid,  tid,  ptid,  *prog)  pid_t pid;  tid_t tid; ptid_t ptid; | Sets Performance Monitor programmation for a target pthread and creates a counting group. | The pm_set_program_pgroup subroutine sets the Performance Monitor programmation for a target | — | S3 |
| pm_set_program_pthread | Subroutine | #include <pmapi.h>   int pm_set_program_pthread ( pid,  tid,  ptid,  *prog)  pid_t pid;  tid_t tid; ptid_t ptid; | Sets Performance Monitor programmation for a target pthread. | The pm_set_program_pthread subroutine sets the Performance Monitor programmation for a target | — | S3 |
| pm_set_program_thread | Subroutine | #include <pmapi.h>   int pm_set_program_thread ( pid,  tid,  *prog)  pid_t pid;  tid_t tid;  pm_prog_t *prog; | Sets Performance Monitor programmation for a target thread. | pid | — | S3 |
| pmc_read_1to4 | Subroutine | #include <pmapi.h> int pmc_read_1to4 (void * buffer) Description The pmc_read_1to4 subroutine reads the registers PMC 1 to PMC 4 into the ad | Reads the performance monitoring counters (PMC) registers PMC 1 to PMC 4 in problem state. | The pmc_read_5to6 subroutine reads the registers PMC 5 and PMC 6 in the same order into the address | — | S3 |
| pmtu | Command | — | Displays and deletes Path MTU discovery related information. | The pmtu command is provided to manage the Path MTU information. The command can be used to | 1. To display Ipv4 pmtu entries, type: pmtu display The output looks similar to the following: | S8 |
| posix_trace_attr_getmaxdatasize | Subroutine | #include <sys/types.h> #include <trace.h> int posix_trace_attr_getmaxdatasize(attr, maxdatasize) const trace_attr_t *restrict attr; size_t * | Retrieves the maximum user trace event data size. | attr | — | S3 |
| posix_trace_attr_setstreamfullpolicy | Subroutine | #include <trace.h> int posix_trace_attr_setstreamfullpolicy(attr,streampolicy) const trace_attr_t *attr; int *streampolicy; Description | Sets the stream full policy. | POSIX_TRACE_LOOP | — | S3 |
| posix_trace_eventid_equal | Subroutine | #include <trace.h> int posix_trace_eventid_equal(trid, event1, event2) trace_id_t trid; trace_event_id_t event1; trace_event_id_t event2; | Compares two trace event type identifiers. | trid | — | S3 |
| posix_trace_get_attr | Subroutine | #include <trace.h> int posix_trace_get_attr(trid, attr) trace_id_t trid; trace_attr_t *attr; Description | Retrieve trace attributes. | trid | — | S3 |
| posix_trace_get_filter | Subroutine | #include <trace.h> int posix_trace_get_filter(trid, set) trace_id_t trid; trace_event_set_t *set; Description | Retrieves the filter of an initialized trace stream. | trid | — | S3 |
| posix_trace_get_status | Subroutine | #include <trace.h> int posix_trace_get_status(trid, statusinfo) trace_id_t trid; struct posix_trace_status_info *statusinfo; Description | Retrieves trace attributes or trace status. | trid | — | S3 |
| prctmp | Command | /usr/sbin/acct/prdaily [ -X ] [  -l ] [ mmdd ] [  -c ] Description The prdaily command is called by the runacct command to format an ASCII r | Displays the session record files. | — | — | S8 |
| priv_clrall | Subroutine | #include <userpriv.h> #include <sys/priv.h> void priv_clrall(privg_t pv) Description The priv_clrall subroutine removes all of the privilege | Removes all of the privilege bits from the privilege set. | pv | — | S3 |
| priv_isnull | Subroutine | #include <userpriv.h> #include <sys/priv.h> int priv_isnull(privg_t pv) Description The priv_isnull subroutine determines whether the privil | Determines if a privilege set is empty. | pv | — | S3 |
| privbit_clr | Subroutine | #include <userpriv.h> #include <sys/priv.h> void privbit_clr(privg_t pv, int priv) Description The privbit_clr subroutine removes the privil | Removes a privilege from a privilege set. | pv | — | S3 |
| proc_mobility_base_set | Subroutine | #include <sys/mobility.h> int  proc_mobility_base_set (pid , flag), pid_t pid; int flag; Description | Sets or unsets attributes used by AIX Live Update to indicate that the current process is a base process. | pid | — | S3 |
| proctree | Command | — | Prints the process tree containing the specified process IDs or users. | The /proc filesystem provides a mechanism to control processes. It also gives access to information | — | S8 |
| projdbfinit | Subroutine | <sys/aacct.h> projdbfinit(void *handle, char *file, int mode) Description The projdbfinit subroutine sets the specified handle to use the sp | Sets the handle to use a local project database as specified in the dbfile pointer and opens the file with | handle | — | S3 |
| ps | Command | X/Open Standards ps [ -A ] [ -M ] [ -N ] [ -Z ] [ -a ] [ -d ] [ -e ] [ -f ] [ -k ] [ -l ] [ -F format] [ -o  Format ] [ -c Clist ] [ -G Glis | Shows status of processes. This document describes the standard AIX ps command and the System V | The ps command writes the status of active processes and if the -m flag is given, displays the associated | — | S8 |
| ps4014 | Command | — | Converts a Tektronix 4014 file to PostScript format. | The ps4014 command reads in a Tektronix 4014 format file and converts it to PostScript format for | — | S8 |
| ps630 | Command | — | Converts Diablo 630 print files to PostScript format. | The ps630 command converts Diablo 630 format print files to PostScript format for printing on a | — | S8 |
| psdanger | Subroutine | # include <signal.h> void psignal ( Signal,  String) int Signal; const char *String; void psiginfo ( Info,  String) | Defines the amount of free paging space available. | — | — | S3 |
| pthread_barrier_wait | Subroutine | — | Synchronizes threads at a barrier. | The pthread_barrier_wait subroutine synchronizes participating threads at the barrier referenced | — | S3 |
| pthread_cancel | Subroutine | #include <pthread.h> int pthread_cancel (thread) pthread_t thread; Description The pthread_cancel subroutine requests the cancellation of th | Requests the cancellation of a thread. | thread | — | S3 |
| pthread_condattr_getpshared | Subroutine | #include <pthread.h> int pthread_condattr_getpshared (attr, pshared) const pthread_condattr_t *attr; int *pshared; Description | Returns the value of the pshared attribute of a condition attributes object. | PTHREAD_PROCESS_SHARED | — | S3 |
| pthread_condattr_setpshared | Subroutine | #include <pthread.h> int pthread_condattr_setpshared (attr, pshared) pthread_condattr_t *attr; int pshared; Description | Sets the value of the pshared attribute of a condition attributes object. | attr | — | S3 |
| pthread_create | Subroutine | #include <pthread.h> int pthread_create (thread, attr, start_routine (void *), arg) pthread_t *thread; const pthread_attr_t *attr; void *(*s | Creates a new thread, initializes its attributes, and makes it runnable. | The pthread_create subroutine creates a new thread and initializes its attributes using the thread | — | S3 |
| pthread_getrusage_np | Subroutine | #include <pthread.h> int pthread_getrusage_np (Ptid, RUsage, Mode) pthread_t Ptid; struct rusage *RUsage; int Mode; | Enable or disable pthread library resource collection, and retrieve resource information for any pthread in | Ptid | — | S3 |
| pthread_mutexattr_setkind_np | Subroutine | #include <pthread.h> int pthread_mutexattr_setkind_np ( attr,  kind) pthread_mutexattr_t *attr; int  kind; Description | Sets the value of the kind attribute of a mutex attributes object. | attr | — | S3 |
| pthread_rwlock_unlock | Subroutine | #include <pthread.h> int pthread_rwlock_unlock (rwlock) pthread_rwlock_t *rwlock; Description The pthread_rwlock_unlock subroutine is called | Unlocks a read-write lock object. | rwlock | — | S3 |
| ptpd | Daemon | — | Starts the Precision Time Protocol (1588-2008) daemon (ptpd). | The ptpd daemon implements the Precision Time Protocol (PTP) version 2 as defined by the IEEE | — | S8 |
| qosadd | Command | — | Adds a QoS (Quality of Service) Service Category or Policy Rule. | The qosadd command adds the specified Service Category or Policy Rule entry in the policyd.conf file | — | S8 |
| qosstat | Command | — | Show Quality of Service (QoS) status. | The qosstat command displays information about the installed Qos policies. Using qosstat without any | 1. qosstat Policy Rule handle 1: Filter specification for rule index 1:         PolicyRulePriority:                    0 | S8 |
| qprt | Command | — | Starts a print job. | The qprt command creates and queues a print job to print the file specified by the File parameter. To print | — | S8 |
| qstatus | Command | — | Provides printer status for the print spooling system. | The qstatus command performs the actual status function for the print-spooling system. This command | — | S8 |
| quick_exit | Subroutine | #include <stdlib.h> _Noreturn void quick_exit(int status); Description The quick_exit subroutine causes normal program termination to occur. | This subroutine causes normal program termination to occur without completely cleaning the resources. | threads.h | — | S3 |
| quot | Command | — | Summarizes file system ownership. | The quot command summarizes file system ownership for JFS file systems by displaying the number of | 1. To display the number of files and bytes owned by each user in the /usr file system, enter: quot -f /usr | S8 |
| rc.powerfail | Command | rc.powerfail [ -h ] \| [ [ -s ] [ -t [ mm ] ][-c [ ss ] ] ] Description The rc.powerfail command is started by the /etc/inittab file when ini | Handles RPA (RS/6000 Platform Architecture) specific EPOW (Environmental and POwer Warning) events | — | 1 These types of errors are considered non-critical cooling problems by the Operating System. rc.powerfail warns the use | S9 |
| refrsrc | Command | — | Refreshes the resources within the specified resource class. | The refrsrc command refreshes the resources within the specified resource class. Use this command | — | S9 |
| refsensor | Command | — | Refreshes a sensor or a microsensor defined to the resource monitoring and control (RMC) subsystem. | The refsensor command refreshes a sensor or microsensor resource that is defined to the RMC | — | S9 |
| removevsd | Command | — | Removes a set of virtual shared disks. | Use this command to remove the logical volumes associated with the virtual shared disks. Volume groups | To unconfigure and remove all defined virtual shared disks in a system or system partition, enter: removevsd -a -f | S9 |
| replacepv | Command | — | Replaces a physical volume in a volume group with another physical volume. | The replacepv command replaces allocated physical partitions and the data they contain from the | 1. To replace physical partitions from hdisk1 to hdisk6, enter: | S9 |
| rexecd | Daemon | — | Provides the server function for the rexec command. | -s | — | S9 |
| rm | Command | — | Removes (unlinks) files or directories. | The rm command removes the entries for the specified File parameter from a directory. If an entry is the | — | S9 |
| rmcondresp | Command | — | Deletes the link between a condition and one or more responses. | The rmcondresp command deletes the link between a condition and one or more responses. A link | — | S9 |
| rmdom | Command | — | Removes the domains from the domain database. | The rmdom command removes the domain that is identified by the Name parameter. The command only | To remove the hrdom domain, type: rmdom hrdom rmf Command Purpose Removes folders and the messages they contain. | S9 |
| rmiscsi | Command | — | Removes iSCSI target data. | The rmiscsi command removes iSCSI target data to ODM. There are two categories of data stored | — | S9 |
| rmm | Command | — | Removes messages from active status. | The rmm command removes messages from active status by renaming them. To rename a message, | — | S9 |
| rmnfsmnt | Command | — | Removes an NFS mount. | The rmnfsmnt command removes the appropriate entry from the /etc/filesystems file and unmounts the | 1. To unmount a file system, enter: rmnfsmnt -f /usr/man -N In this example, the /usr/man file system is unmounted. 2. T | S9 |
| rmproj | Subroutine | <sys/aacct.h> rmproj(struct project *, int flag) Description The rmproj subroutine removes the definition of a project from kernel project r | Removes project definition from kernel project registry. | project | — | S3 |
| rmprojdb | Subroutine | <sys/aacct.h> rmprojdb(void *handle, struct project *project, int flag) Description The rmprojdb subroutine removes the project definition s | Removes the specified project definition from the specified project database. | handle | — | S3 |
| rmrpdomain | Command | — | Removes a peer domain that has already been defined. | The rmrpdomain command removes the peer domain definition that is specified by the peer_domain | — | S9 |
| rmserver | Command | rmsmbcmnt -f MountPoint [-B \| -N \| -I] Description The rmsmbcmnt command removes an SMB client file system entry from the /etc/filesystems f | Removes a subserver definition from the Subserver Type object class. | — | — | S9 |
| rmts | Command | — | Removes a thin server. | The rmts command removes a thin server specified by ThinServer and created with the mkts command. | 1. To remove a thin server named lobo, enter: rmts lobo  Location /usr/sbin/rmts Files | S9 |
| rmtun | Command | rmusil -R RelocatePath -r Description The rmusil command removes an existing USIL instance. | Deactivates operational tunnel(s) and optionally removes tunnel definition(s). | — | — | S9 |
| rmvirprt | Command | — | Removes a virtual printer. | The rmvirprt command removes the virtual printer assigned to the PrinterQueueName and | To remove the attribute values for the mypro virtual printer associated with the proq print queue, type: rmvirprt  -d my | S9 |
| rrestore | Command | — | Copies previously backed up file systems from a remote machine's device to the local machine. | The rrestore command restores Version 3 by i-node backups from a remote machine's device to a file | — | S9 |
| rs_info | Subroutine | #include <sys/rset.h> long rs_info(void *out, long command, long arg1, long arg2) Description The rs_info subroutine returns affinity system | Retrieves system affinity information. | out | — | S3 |
| rs_setpartition | Subroutine | include <sys/rset.h> int rs_setpartition(pid, rset, flags) pid_t pid; rsethandle_t rset; unsigned int flags; | Sets the partition resource set of a process. | — | — | S3 |
| rtl_enable | Command | — | Relinks shared objects to enable the runtime linker to use them. | The rtl_enable command relinks a module, or an archive containing modules, with the -G flag, to enable | — | S9 |
| rusers | Command | — | Reports a list of users logged on to remote machines. | The rusers command produces a list of users who are logged on to remote machines. The rusers | 1. To produce a list of the users on your network that are logged in remote machines, enter: rusers 2. To produce a list | S9 |
| sccs | Command | — | Administration program for SCCS commands. | The sccs command is an administration program that incorporates the set of Source Code Control System | — | S9 |
| sched_setscheduler | Subroutine | #include <sched.h> int sched_setscheduler (pid, policy, param) pid_t pid; int policy; const struct sched_param *param; | Sets the scheduling policy and parameters. | — | — | S3 |
| sec_getsyslab | Subroutine | #include <sys/mac.h> int sec_getsyslab (minsl, maxsl, mintl, maxtl) sl_t *minsl; sl_t *maxsl; tl_t *mintl; | Gets the system sensitivity and integrity labels. | The sec_getsyslab subroutine gets the system minimum and maximum sensitivity labels and the system | — | S3 |
| sec_setmsglab | Subroutine | #include <sys/mac.h> #include <sys/ipc.h> #include <sys/msg.h> int sec_setmsglab (msgid, sl, tl) int msgid; | Sets the security attributes of an Interprocess Communication (IPC) message queue. | The sec_setmsglab subroutine sets the security attributes of the message queue that is specified by the | — | S3 |
| sec_setsemlab | Subroutine | #include <sys/mac.h> #include <sys/ipc.h> #include <sys/sem.h> int sec_setsemlab (semid, sl, tl) int semid; | Sets the security attributes for a semaphore. | The sec_setsemlab subroutine sets the security attributes of the semaphore that is specified by the | — | S3 |
| sectoldif | Command | — | Prints users and groups defined locally to stdout in ldif format. | The sectoldif command reads users and groups defined locally, and prints the result to stdout in ldif | 1. To print all users and groups defined locally, enter the following: sectoldif -d cn=aixsecdb,cn=aixdata -S rfc2307aix | S9 |
| sem_destroy | Subroutine | #include <semaphore.h> int sem_destroy (sem) sem_t *sem; Description The sem_destroy subroutine destroys the unnamed semaphore indicated by | Destroys an unnamed semaphore. | sem | — | S3 |
| sem_open | Subroutine | #include <semaphore.h> sem_t * sem_open (const char *name, int oflag, mode_t mode, unsigned value) Description The sem_open subroutine estab | Initializes and opens a named semaphore. | name | — | S3 |
| setcsmap | Subroutine | #include <sys/termios.h> int setcsmap (Path); char * Path; Description The setcsmap subroutine reads in a code-set map file. The path parame | Reads a code-set map file and assigns it to the standard input device. | Path | — | S3 |
| setea | Subroutine | — | Sets an extended attribute value. | Extended attributes are name:value pairs associated with the file system objects (such as files, | — | S3 |
| setpenv | Subroutine | #include <usersec.h> int setpenv ( User,  Mode,  Environment,  Command) char *User; int Mode; char **Environment; char *Command; Description | Sets the current process environment. | Setting the Process Environment | — | S3 |
| setuname | Command | — | Sets the node name of the system. | The setuname command is used to set the node name of the system. The -n option must be specified. | 1. To temporarily change the node name to "orion", enter: setuname -t -n orion 2. To permanently change the node name to | S9 |
| sh | Command | shconf -d shconf -R -l Name shconf {-D [-O ] \| -E [-O ]} [-H] -l Name shconf -l Name [-a Attribute=Value] ... Description | Invokes the default shell. | pp_cmd | — | S9 |
| shell | Command | — | Executes a shell with the user's default credentials and environment. | The shell command re-initializes a user's login session. When the command is given, the port | To re-initialize your session to your default credentials and environment after using the trusted shell (tsh), enter: sh | S9 |
| shm_open | Subroutine | #include <sys/mman.h> int shm_open (name, oflag, mode) const char *name; int oflag; mode_t mode; | Opens a shared memory object. | name | — | S3 |
| sigstack | Subroutine | #include <signal.h>   int sigstack ( InStack,  OutStack) struct sigstack *InStack, *OutStack; Description | Sets and gets signal stack context. | InStack | — | S3 |
| snmptrap | Command | — | Generate a notification (trap) to report an event to the SNMP manager with the specified message. | Generate a notification (trap) to report an event to the SNMP manager with the specified message. | 1. To send a trap with the message 'hello world' to the SNMP agent running on the local host, enter the following: | S9 |
| SpmiDdsDelCx | Subroutine | #include sys/Spmidef.h int SpmiDdsDelCx(Area) char *Area; Description The SpmiDdsDelCx subroutine informs the SPMI that a previously added, | Deletes a volatile context. | — | — | S3 |
| SpmiExit | Subroutine | #include sys/Spmidef.h void SpmiExit() Description A successful “SpmiInit Subroutine” on page 2019 or “SpmiDdsInit Subroutine” on page 2003 | Terminates a dynamic data supplier (DDS) or local data consumer program's association with the SPMI, | /usr/include/sys/Spmidef.h | — | S3 |
| SpmiGetStat | Subroutine | #include sys/Spmidef.h struct SpmiStat *SpmiGetStat(StatHandle) SpmiStatHdl StatHandle; Description The SpmiGetStat subroutine returns a poi | Returns a pointer to the SpmiStat structure corresponding to a specified statistic handle. | /usr/include/sys/Spmidef.h | — | S3 |
| SpmiGetStatSet | Subroutine | #include sys/Spmidef.h int SpmiGetStatSet(StatSet, Force); struct SpmiStatSet *StatSet; boolean Force; Description | Requests the SPMI to read the data values for all statistics belonging to a specified set. | — | — | S3 |
| SpmiInstantiate | Subroutine | #include sys/Spmidef.h int SpmiInstantiate(CxHandle) SpmiCxHdl CxHandle; Description The SpmiInstantiate subroutine explicitly instantiates | Explicitly instantiates the subcontexts of an instantiable context. | /usr/include/sys/Spmidef.h | — | S3 |
| SpmiNextCx | Subroutine | #include sys/Spmidef.h struct SpmiCxLink *SpmiNextCx(CxLink )struct SpmiCxLink *CxLink; Description The SpmiNextCx subroutine locates the ne | Locates the next subcontext of a context. | /usr/include/sys/Spmidef.h | — | S3 |
| SpmiNextHot | Subroutine | #include sys/Spmidef.h struct SpmiHotVals *SpmiNextHot(HotSet, HotVals) struct SpmiHotSet *HotSet; struct SpmiHotVals *HotVals; Description | Locates the next set of peer statistics SpmiHotVals belonging to an SpmiHotSet. | — | — | S3 |
| SpmiNextValue | Subroutine | #include sys/Spmidef.h struct SpmiStatVals*SpmiNextValue( StatSet, StatVal, value) struct SpmiStatSet *StatSet; struct SpmiStatVals *StatVal | Returns either the first SpmiStatVals structure in a set of statistics or the next SpmiStatVals structure in | — | — | S3 |
| SpmiPathAddSetStat | Subroutine | #include sys/Spmidef.h struct SpmiStatVals *SpmiPathAddSetStat(StatSet, StatName, Parent) struct SpmiStatSet *StatSet; char *StatName; | Adds a statistics value to a set of statistics. | The SpmiPathAddSetStat subroutine adds a statistics value to a set of statistics. The SpmiStatSet | — | S3 |
| srcsrqt | Subroutine | #include <spc.h> srcsrqt(Host, SubsystemName, SubsystemPID, RequestLength, SubsystemRequest, ReplyLength, ReplyBuffer, StartItAlso,  Continu | Sends a request to a subsystem. | The srcsrqt subroutine sends a request to a subsystem, waits for a response, and returns one or | — | S3 |
| srcstat_r | Subroutine | #include <spc.h> int srcstat_r(Host, SubsystemName, SubsystemPID, ReplyLength,                  StatusReply, Continued, SRCHandle) char * Ho | Gets short status on a subsystem. | The srcstat_r subroutine sends a short status request to the System Resource Controller (SRC) and | — | S3 |
| startrpdomain | Command | — | Brings a peer domain that has already been defined online. | The startrpdomain command brings a defined peer domain online by starting the resources on each | — | S9 |
| stoprpdomain | Command | — | Takes an online peer domain offline. | The stoprpdomain command takes all of the nodes that are currently online in the peer domain offline. | — | S9 |
| strtune | Command | strtune {-n name \| -q addr} -o tunable_name[=value] -o tunable_name[=value] ... strtune [-n name \| -q addr [-a]] -o trclevel[=value] strtune | This command has several related functions: | — | — | S9 |
| struct | Command | — | Translates a FORTRAN program into a RATFOR program. | The struct command translates the FORTRAN program specified by File (standard input default) into | — | S9 |
| subwin | Subroutine | #include <curses.h> WINDOW *subwin (ParentWindow, NumLines, NumCols,Line,Column) WINDOW * ParentWindow ; int NumLines, NumCols, Line, Column | Creates a subwindow within an existing window. | NumCols | — | S3 |
| svmon | Command | — | Captures and analyzes a snapshot of virtual memory. | The svmon command displays information about the current state of memory. However, the displayed | — | S9 |
| swap | Command | — | Provides a paging space administrative interface. | The functions provided by the swap command are display of characteristics, addition of paging space and | — | S9 |
| swts | Command | — | Switches a thin server to a different COSI. | The swts command switches a thin server to a different Common Operating System Image (COSI). If | 1. To switch the cosi1 common image of a thin server named lobo to a common image named cosi2, enter: swts -c cosi2 lobo | S9 |
| sysconf | Subroutine | #include <unistd.h> long int sysconf ( Name) int Name; Description The sysconf subroutine determines the current value of certain system par | Determines the current value of a specified system limit or option. | Name | — | S3 |
| sysdumpstart | Command | — | Provides a command line interface to start a kernel dump to the primary or secondary dump device. | The sysdumpstart command provides a command line interface to start a kernel dump to the primary or | — | S9 |
| tcbck | Command | — | Audits the security state of the system. | The tcbck command audits the security state of the system by checking the installation of the files | — | S9 |
| tcsetattr | Subroutine | #include <termios.h> int tcsetattr (FileDescriptor, OptionalActions, TermiosPointer) int  FileDescriptor,  OptionalActions; const struct ter | Sets terminal state. | FileDescriptor | — | S3 |
| tgoto | Subroutine | #include <curses.h> #include <term.h> char *tgoto( Capability,  Column,  Row) char *Capability; int Column, Row; | Duplicates the tparm subroutine. | Capability | — | S3 |
| thrd_equal | Subroutine | #include <threads.h> int thrd_equal(thrd_t thr0, thrd_t thr1); Description The thrd_equal subroutine determines whether the thread identifie | This subroutine compares two threads. | thr0 | — | S3 |
| timed | Daemon | — | Invokes the time server daemon. | The timed daemon synchronizes one machine's clock with those of other machines on the local area | — | S9 |
| topsvcsctrl | Command | — | Starts the topology services subsystem. | The topsvcsctrl control script controls the operation of the topology services subsystem. The | — | S9 |
| touchwin | Subroutine | #include <curses.h> touchwin( Window) WINDOW *Window; Description The touchwin (“touchwin Subroutine” on page 2196) subroutine forces every | Forces every character in a window's buffer to be refreshed at the next call to the wrefresh subroutine. | Window | To refresh a user-defined parent window, parent_window, that has been edited through its subwindows, use: WINDOW *parent | S3 |
| tpm_activate | Command | — | Changes the Trusted Platform Module (TPM) active states. | The tpm_activate command reports the status of the TPM flags regarding the active state of the TPM. | — | S9 |
| tpm_createek | Command | tpm_enable [ -e ] [ -d ] [ -h ] [ -l [ none \| error \| info \| debug ] ] [ -o ] [ -s ] [ -u ] [ -v ] [ -z ] Description The tpm_enable command | Creates an endorsement key pair on the Trusted Platform Module (TPM). | — | — | S9 |
| trc_strerror | Subroutine | #include <sys/libtrace.h> char *trc_strerror (handle, rv) void *handle; int rv; Description | Returns the error message, or next error message, associated with a trace log object or trc_loginfo object. | handle | 1. To retrieve all error messages from a call to the trc_open subroutine, call the trc_strerror subroutine as follows: { | S3 |
| trcstop | Subroutine | # include <sys/trcmacros.h> # define TRCSTOP SERIAL 0x40000000 # define TRCSTOP DISCARDBUFS 0x20000000 int trcstop( Channel) int Channel; | Stops a trace session. | Channel | — | S3 |
| trcupdate | Command | — | Adds, replaces, or deletes trace report format templates. | The trcupdate command adds, replaces, or deletes trace report format templates in the /etc/trcfmt or | — | S9 |
| trpt | Command | — | Performs protocol tracing on TCP sockets. | The trpt command queries the buffer for Transmission Control Protocol (TCP) trace records. This buffer is | — | S9 |
| truss | Command | truss [ -f] [ -c] [ -a] [ -l ] [ -d ] [ -D ] [ -e] [ -i] [ { -t \| -x} [!] Syscall [...] ] [ -s [!] Signal [...] ] [ { -m }[!] Fault  [...]] | Traces a process's system calls, dynamically loaded user level function calls, received signals, and | — | — | S9 |
| turnoff | Command | turnon Description The turnon command sets the permission codes of files in the /usr/games directory. Root user authority is required to run | Sets the permission codes off for files in the /usr/games directory. | /usr/games | — | S9 |
| twconvfont | Command | — | Converts other font files to a BDF font file. | The twconvfont command converts one font file type to the BDF font file. The supported code pages are | To convert the font file USRFONT.C12 to a BDF font file of code page of type SOPS with the name user.bdf, enter: twconvf | S9 |
| type | Command | — | Writes a description of the command type. | The standard output of the type command contains information about the specified command and | 1. To learn whether the cd command is a base command or an alias or some other command type, enter: type cd The screen d | S9 |
| tzupg.pl | Command | tzupg.pl Description The tzupg.pl command updates the time zone information on an AIX system based on the latest Olson time zone database fr | Updates the time zone database of the AIX system by using the latest Olson time zone database from the | — | The tzupg.pl tool displays the main menu and prompts you to select the following options from the main menu: $ tzupg.pl | S9 |
| umountall | Command | — | Unmounts groups of dismountable devices or filesystems. | The umountall command by default unmounts all dismountable file systems or devices except root, / | — | S9 |
| units | Command | — | Converts units in one measure to equivalent units in another measure. | The units command converts quantities expressed in one measurement to their equivalents in another. | — | S9 |
| unmirrorvg | Command | — | Removes the mirrors that exist on volume groups or specified disks. | The unmirrorvg command unmirrors all the logical volumes detected on a given volume group. This | — | S9 |
| unpack | Command | — | Expands files. | The unpack command expands files created by the pack command. For each file specified, the unpack | To unpack packed files: unpack chap1.z chap2 This expands the packed files chap1.z and chap2.z, and replaces them with f | S9 |
| update_iscsi | Command | update_iscsi [ -l name ] Description The update_iscsi command lists and updates the devices for which configuration attributes are related t | Lists and updates the configurations of devices for the iSCSI software initiator that is accessed through | -l | — | S9 |
| usermod | Command | — | Changes user attributes. | Attention: Do not use the usermod command if you have a Network Information Service (NIS) | — | S9 |
| usrinfo | Subroutine | #include <uinfo.h> int usrinfo ( Command,  Buffer,  Count) int Command; char *Buffer; int Count; | Gets and sets user information about the owner of the current process. | Command | — | S3 |
| usrrpt | Command | — | Reports the security capabilities of users. | The usrrpt command reports security capability information of users such as privileged commands | — | S9 |
| uutry | Command | uutry [  -xDebugLevel ] [  -r ] SystemName Description The uutry command contacts a remote system, specified by the SystemName parameter, us | Contacts a specified remote system with debugging turned on and allows the user to override the default | -r | 1. To change the amount of detail the uutry command provides about the progress of the uucico operation, use the -x flag | S9 |
| vwsprintf | Subroutine | #include <wchar.h> #include <stdarg.h> int vwsprintf (wcs, Format, arg) wchar_t * wcs; const char * Format; | Writes formatted wide characters. | The vwsprintf subroutine writes formatted wide characters. It is structured like the vsprintf subroutine | — | S3 |
| watch | Command | — | Observes a program that might be untrustworthy. | The watch command allows the root user or a member of the audit group to observe the actions of a | — | S9 |
| wcstok | Subroutine | #include <wchar.h> wchar_t *wcstok ( WcString1,  WcString2,  ptr) wchar_t *WcString1; const wchar_t *WcString2; wchar_t **ptr | Converts wide-character strings to tokens. | ptr | — | S3 |
| wlm_assign | Subroutine | #include <sys/wlm.h> int wlm_assign ( args) struct wlm_assign *args; Description The wlm_assign subroutine: | Manually assigns processes to a class or cancels prior manual assignments for processes. | — | — | S3 |
| wlm_endkey | Subroutine | #include sys/wlm.h int wlm_endkey(struct wlm_args *args, void *ctx) Description The wlm_endkey subroutine frees the classes to the keys tran | Frees the classes to keys translation table. | - ctx | — | S3 |
| writesrv | Daemon | /usr/sbin/acct/wtmpfix [  File ... ] | Allows users to send messages to and receive messages from a remote system. | — | — | S9 |
| wstring | Subroutine | #include <wstring.h>   wchar_t *wstrcat (“wstring Subroutine” on page 2388) (XString1, XString2) wchar_t *XString1, *XString2; | Perform operations on wide character strings. | — | — | S3 |
| x_add_nfs_fpe | Command | — | Adds a NFS/TFTP accessed font directory to a font path. | The x_add_nfs_fpe command adds a font path element to the font path of the selected network type | To add the fonts in /usr/lib/X11/fonts/100dpi to the network type x_st_mgr.ether, enter: x_add_nfs_fpe cedar /usr/lib/X1 | S9 |
| xpr | Command | — | Formats a window dump file for output to a printer. | The xpr command uses a window dump file produced by the xwd utility as input and formats the dump | — | S9 |
| xrdb | Command | — | X Server resource database utilities. | The xrdb command gets or sets the contents of the RESOURCE_MANAGER property on the root window | — | S9 |
| xss | Command | — | Improves the security of unattended workstations. | The xss command works with the newly added Massachusetts Institute of Technology (MIT) Screen Saver | When running remotely and using the -display flag for the xss command, remember that you may also have to use the -displ | S9 |
| xwud | Command | — | Retrieves and displays the dumped image of an Enhanced X-Windows window. | The xwud command retrieves the dumped image of an Enhanced X-Windows window. It does so by | — | S9 |
| ypxfr | Command | — | Transfers a Network Information Services (NIS) map from an NIS server to a local host. | The ypxfr command transfers a Network Information Services (NIS) map from an NIS server to the local | — | S9 |

[← AIX 7.3 トップへ](index.md)
